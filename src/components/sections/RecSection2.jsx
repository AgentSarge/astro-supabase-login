export default function RecSection2({ selectedRole, selectedLocation, selectedOffice }) {
  return (
    <div style={{
      height: '100%',
      background: 'var(--bg-primary)',
      padding: '24px',
      overflow: 'hidden'
    }}>
      <div style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100%',
        color: 'var(--text-secondary)',
        fontFamily: '"Geist", "Inter", sans-serif'
      }}>
        Rec Section 2
      </div>
    </div>
  );
} 